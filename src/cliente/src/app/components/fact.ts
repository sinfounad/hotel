import { StringifyOptions } from 'querystring';

export interface Fact {
    _id:"";
    nombre: string;
    documento: string;
    direccion: string;
    correo: string;
    telefono: string;
    habitacion: string;
    entrada: string;
    salida: string;
}
